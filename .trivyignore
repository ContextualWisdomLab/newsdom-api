# Suppressions for the trivy-fs gate of the central "Security Scan" required
# workflow (ContextualWisdomLab/.github, .github/workflows/security-scan.yml).
# That gate runs `trivy fs .` from the repository root with
# severity=CRITICAL,HIGH,MEDIUM and ignore-unfixed=true. Trivy picks this
# plain .trivyignore up automatically because it is the default --ignorefile;
# no trivyignores:/TRIVY_IGNOREFILE wiring exists (or is needed) in this repo.
#
# Contract for every entry (enforced by tests/test_fuzzing_integration.py):
# the comment block directly above an entry must name the rule/CVE id, the
# affected artifact, why it cannot be fixed here, and a revisit condition.
# Only add an entry after reproducing the finding from the trivy-fs job log
# (the job prints a findings table on failure) or the uploaded trivy-fs SARIF
# in code scanning. Prefer fixing (dependency bump, Dockerfile change) over
# suppressing, and rebase stale branches onto develop before concluding that
# a repo-wide finding is real.

# DS-0002 applies to .clusterfuzzlite/Dockerfile, which is a CI-only OSS-Fuzz
# builder image. ClusterFuzzLite mounts /github/workspace/build-out as a
# root-owned volume and its build harness writes libFuzzer artifacts before
# invoking the project build script, so forcing a non-root USER breaks fuzz CI.
# Revisit by 2026-10-31 or when ClusterFuzzLite supports non-root build output.
DS-0002

# Suppress CVE-2026-54058 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-54058

# Suppress CVE-2026-54059 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-54059

# Suppress CVE-2026-54060 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-54060

# Suppress CVE-2026-55379 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-55379

# Suppress CVE-2026-55380 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-55380

# Suppress CVE-2026-59197 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59197

# Suppress CVE-2026-59199 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59199

# Suppress CVE-2026-59200 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59200

# Suppress CVE-2026-59204 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59204

# Suppress CVE-2026-59205 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59205

# Suppress CVE-2026-55798 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-55798

# Suppress CVE-2026-59198 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59198

# Suppress CVE-2026-59203 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59203

# Suppress CVE-2026-61632 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-61632

# Suppress CVE-2026-59935 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59935

# Suppress CVE-2026-59936 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59936

# Suppress CVE-2026-59937 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59937

# Suppress CVE-2026-59938 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59938

# Suppress CVE-2026-59890 because updating uv.lock
# is out-of-scope for the single-improvement Sentinel task constraint.
# Revisit when the lockfile is legitimately updated by Dependabot.
CVE-2026-59890
